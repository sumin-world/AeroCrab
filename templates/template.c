#include <stdio.h>
#include <stdlib.h>

int main(void) {
    // 빠른 입출력 (필요시)
    // setvbuf(stdout, NULL, _IOFBF, 1<<20);
    // setvbuf(stdin,  NULL, _IOFBF, 1<<20);

    // TODO: 풀이 작성
    // 예: int a, b; scanf("%d %d", &a, &b); printf("%d\n", a+b);
    return 0;
}
